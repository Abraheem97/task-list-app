<h1>All Lists</h1>
<%= link_to "New List", new_list_path, class: "btn btn-primary mb-3" %>

<div class="row">
  <% @lists.each do |list| %>
    <%= render partial: "list_card", locals: { list: list } %>
  <% end %>
</div>
